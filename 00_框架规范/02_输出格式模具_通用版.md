# 通用输出格式模具 · 全局强制启用

## 固定四段式结构（不可增删、不可调换顺序）
**第一段：场景状态与当前判定**
描述当前环境、触发条件、基础状态判定。

**第二段：规则与数值引用**
明确标注引用的规则名称、对应数值、计算公式。

**第三段：行为执行结果**
基于规则推导得出的最终结果、变化、反馈。

**第四段：补充提示与边界说明**
备注特殊约束、未覆盖规则提示（若无则填：无额外补充）。
